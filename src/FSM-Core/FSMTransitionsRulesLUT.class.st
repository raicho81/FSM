"
FSMTransitions - Dictionary holding the state transitions of the FSM used as a transitions LUT when a new FSMInputSymbol fed to the FSM for evaluation.
"
Class {
	#name : #FSMTransitionsRulesLUT,
	#superclass : #Dictionary,
	#category : #'FSM-Core-Base'
}
